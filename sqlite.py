import sqlite3

# SQLite 데이터베이스 연결
#conn = sqlite3.connect('./login.db')
#cursor = conn.cursor()

# 사용자 정보 추가
#cursor.execute("INSERT INTO users (id, pw) VALUES (?, ?)", (1, "password123"))
#cursor.execute("INSERT INTO users (id, pw) VALUES (?, ?)", (2, "secret456"))

# 커밋하여 변경 내용을 저장
#conn.commit()

# SQLite 연결 닫기
#conn.close()
